[project]
name = "gpu-hunter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "thedeadbyte", email = "deadbyte@deadbyte.co" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
gpu-hunter = "gpu_hunter:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
